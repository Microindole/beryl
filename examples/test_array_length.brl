int main() {
    var arr: [5]int = [10, 20, 30, 40, 50]
    
    // Print length directly
    print(arr.length)
    
    // Use length in loop
    print(999) // Separator
    for var i = 0; i < arr.length; i = i + 1 {
        print(arr[i])
    }
    
    return 0
}
