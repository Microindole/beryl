// tests/example/parser/match_payload_pattern.lcy
// Parser regression: enum payload binders in match pattern.

enum Message {
  Quit,
  Text(string),
  Pair(int, string)
}

var m = Pair(1, "x")
var y = match (m) {
  Quit => 0,
  Text(msg) => 1,
  Pair(a, b) => a
}
return y
