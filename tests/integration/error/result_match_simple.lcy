// 最简单的 Result match 测试

struct Error {
    string message
}

int! test_func() {
    return Ok(42)
}

int main() {
    var res = test_func()
    
    var val = match res {
        case Result.Ok(v) => v,
        case Result.Err(_) => 0
    }
    
    print(val)
    return 0
}
